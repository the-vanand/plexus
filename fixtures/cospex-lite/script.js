const header=document.getElementById('siteHeader');const menu=document.querySelector('.menu-toggle');const nav=document.getElementById('nav');const form=document.getElementById('inquiryForm');const status=document.getElementById('formStatus');
const updateHeader=()=>header.classList.toggle('scrolled',window.scrollY>24);updateHeader();window.addEventListener('scroll',updateHeader,{passive:true});
menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));header.classList.toggle('open',!open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');header.classList.remove('open')}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelectorAll('[data-collection]').forEach(link=>link.addEventListener('click',()=>{document.getElementById('collection').value=link.dataset.collection}));
form.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const data=new FormData(form);const subject=encodeURIComponent(`COSPEX private enquiry — ${data.get('collection')}`);const body=encodeURIComponent(`Full name: ${data.get('name')}\nEmail: ${data.get('email')}\nPhone: ${data.get('phone')}\nCollection: ${data.get('collection')}\nApproximate budget: ${data.get('budget')}\n\nStory / message:\n${data.get('message')||'—'}`);status.textContent='Opening your email application…';window.location.href=`mailto:mail@COSPEX.com?subject=${subject}&body=${body}`;setTimeout(()=>status.textContent='Thank you. Complete the prepared email to send your private enquiry to COSPEX.',700)});
document.getElementById('year').textContent=new Date().getFullYear();
